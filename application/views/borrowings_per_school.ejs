<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= pageTitle%></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 20px;
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    th, td {
      padding: 12px 15px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }

    th {
      background-color: #f5f5f5;
      font-weight: bold;
    }

    tbody tr:hover {
      background-color: #f9f9f9;
    }
  </style>
</head>
<body>
  <h1><%= pageTitle %></h1>

  <table>
    <thead>
      <tr>
        <th>School</th>
        <th>Borrowings Count</th>
      </tr>
    </thead>
    <tbody>
      <% schools.forEach((book_borrowings_count_per_school,_) => { %>
        <tr>
          <td><%= book_borrowings_count_per_school.school_name %></td>
          <td><%= book_borrowings_count_per_school.books %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
</body>
</html>
